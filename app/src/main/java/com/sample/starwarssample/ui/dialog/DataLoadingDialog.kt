package com.sample.starwarssample.ui.dialog

import android.content.Context
import com.sample.starwarssample.R

class DataLoadingDialog(context: Context) : BaseDialogHelper(context) {
    override val layoutId = R.layout.dialog_loading_data
}